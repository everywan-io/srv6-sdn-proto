syntax = "proto3";

/* Status codes */
enum StatusCode {
  STATUS_OK = 200;
  STATUS_BAD_REQUEST = 400;
  STATUS_INTERNAL_SERVER_ERROR = 500;
}

/* Define a status in a reply message */
message Status {
  StatusCode code = 1;
  string reason = 2;
}